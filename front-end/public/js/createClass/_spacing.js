export const spacingClasses = (classParts = [], className = "") => {

  // p[x/y/t/r/b/l]-[max/min]_{breakpoint}-value
  // m[x/y/t/r/b/l]-[max/min]_{breakpoint}-value

  const properties = [];
  const vals = [];






}